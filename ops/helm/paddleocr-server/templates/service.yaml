apiVersion: v1
kind: Service
metadata:
  name: {{ include "paddleocr-server.fullname" . }}
  labels:
    {{- include "paddleocr-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "paddleocr-server.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
