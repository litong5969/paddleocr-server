# Agents Routing Rules

- 路由策略：prefix-first，未命中前缀时默认 DEL-PM。
- 前缀定义：见 `kernel/AGENTS.md` 与 `kernel/runtime/mcp/registry.yml`。
- Anti-Fake-Work：无可验证产出不得宣称完成。
- 维护：新增/调整 Agent 时同时更新 registry、policies、TEAM_OVERVIEW、TASK_AGENT_MATRIX。
